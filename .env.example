# Local-only values for one-time scripts. Copy to .env locally; never commit it.
# Run scripts with: node --env-file=.env scripts/create-admin.mjs
FIREBASE_API_KEY=
ADMIN_EMAIL=
ADMIN_USERNAME=
ADMIN_PASSWORD=
ADMIN_DISPLAY_NAME=
